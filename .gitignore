# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Variáveis de ambiente — nunca commitar com valores reais
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Vitest coverage
coverage

# Estado local de agentes/worktrees do Claude Code
.claude/

# Ferramentas temporárias de QA/mutação — nunca commitar
# Harness de mutação por transform em memória, criado por agentes de QA. O próprio arquivo
# traz "NÃO COMMITAR" no cabeçalho; ignorar é a trava estrutural (demanda 123, AUTO-2).
vitest.*-mut.config.ts
vitest.qa*.config.ts

# Diretório de trabalho do driver de navegador usado em QA headed
.playwright-cli/
